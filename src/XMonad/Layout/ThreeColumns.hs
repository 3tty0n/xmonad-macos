-- Contrib-compatible module path. The layouts themselves live in
-- XMonad.Layout, which XMonad re-exports, so this import is optional.
module XMonad.Layout.ThreeColumns (ThreeCol(..)) where
import XMonad.Layout (ThreeCol(..))

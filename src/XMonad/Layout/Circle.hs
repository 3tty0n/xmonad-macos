-- Contrib-compatible module path. The layout itself lives in XMonad.Layout,
-- which XMonad re-exports, so this import is optional.
module XMonad.Layout.Circle (Circle(CircleRatio,Circle),circleDelta,circleFrac) where
import XMonad.Layout (Circle(CircleRatio,Circle),circleDelta,circleFrac)
